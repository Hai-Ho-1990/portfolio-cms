import React from 'react';

export default function WorksHeader() {
    return (
        <header className="fixed w-screen   text-[#c4b8a5] flex flex-col items-center  pt-10  justify-center hidden-overflow">
            <h1 className="text-sm lg:text-base uppercase font-semibold">
                my projects
            </h1>
        </header>
    );
}
