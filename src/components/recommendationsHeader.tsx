import React from 'react';

export default function RecommendationsHeader() {
    return (
        <header className="lg:fixed w-screen   text-[#c4b8a5] flex flex-col items-center  pt-14  justify-center hidden-overflow">
            <h1 className="text-sm lg:text-lg uppercase font-semibold">
                some kind words
            </h1>
        </header>
    );
}
